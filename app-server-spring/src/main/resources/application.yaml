spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store_api?createDatabaseIfNotExist=true
    username: root
    password: rootroot
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExp: 300 #5m
    refreshTokenExp: 600 #10m